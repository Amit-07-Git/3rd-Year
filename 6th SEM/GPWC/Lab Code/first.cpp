#include<SFML/Graphics.hpp>
using namespace sf;

int main(){
  VideoMode vm(1920, 1080);
  RenderWindow window(vm, "Circle");
  while(window.isOpen()){
  //handle the players
  //updates scene
  //draw scene
  }
  return 0;
}
